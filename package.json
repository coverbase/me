{
    "name": "moritzmueller",
    "private": true,
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "scripts": {
        "dev": "npm run dev -w apps",
        "build": "npm run build -w apps"
    },
    "devDependencies": {
        "prettier": "^3.0.3",
        "prettier-plugin-tailwindcss": "^0.5.4",
        "typescript": "^5.2.2",
        "unocss": "^0.56.0"
    }
}
